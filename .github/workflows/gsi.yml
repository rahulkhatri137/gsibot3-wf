name: GSIBot3 automated
 
on:
  push:
  workflow_dispatch:

env:
  TYPE: build
  TZ: Asia/Kolkata
jobs:
  Bot3:
    runs-on: ubuntu-20.04
    timeout-minutes: 525600

    steps:
       - name: Checkouts
         uses: actions/checkout@master
       - uses: rokibhasansagar/slimhub_actions@main
         
       - name: Initializing environment
         continue-on-error: true
         timeout-minutes: 100
         run: |
              sudo -E apt-get -qq update
              sudo -E apt-get -qq install bc build-essential zip curl libstdc++6 git wget python gcc clang libssl-dev rsync flex bison ccache openjdk-8-jdk expect aria2
              git config --global user.name "rahulkhatri137"
              git config --global user.email "${{ secrets.GHMAIL }}"
                   
       - name: Setup Erfan Tools
         timeout-minutes: 10
         run: |
              git clone --recurse-submodules https://github.com/rahulkhatri137/ErfanGSIs
              sudo chmod -R 0777 ErfanGSIs
              sudo bash ErfanGSIs/setup.sh

       - name: Setup XiaoxindadaSGSIs Tools
         timeout-minutes: 10
         run: |
              git clone --recurse-submodules https://github.com/rahulkhatri137/SGSI-build-tool.git -b 12 XiaoxindadaSGSIs
              sudo chmod -R 0777 XiaoxindadaSGSIs
              sudo sed -i 's/USE_MIRROR_FOR_PIP\=true/USE_MIRROR_FOR_PIP\=false/' XiaoxindadaSGSIs/setup.sh
              sudo bash XiaoxindadaSGSIs/setup.sh
              java -version
         
       - name: Add values
         timeout-minutes: 1
         run: |
              mkdir configs && cd configs
              touch configs.prop
              touch allowed2port.json
              echo "bot-token=${{ secrets.BOTKEY }}" >> configs.prop
              echo "bot-username=${{ secrets.BOTUN }}" >> configs.prop
              echo "bot-master=${{ secrets.USERKEY }}" >> configs.prop
              echo "privateChat=-1001412098793" >> configs.prop
              echo "requestChat=-1001509399593" >> configs.prop
              echo "publicChannel=RK137GSI" >> configs.prop
              echo "bot-sf-pass=${{ secrets.SFPASS }}" >> sf-creds.prop
              echo "bot-sf-proj=${{ secrets.PROJECT }}" >> sf-creds.prop
              echo "bot-sf-user=${{ secrets.SFUSER }}" >> sf-creds.prop
              echo "bot-announcement-id=-1001500771995" >> sf-creds.prop
              echo "bot-sf-host=frs.sourceforge.net" >> sf-creds.prop
              echo "bot-send-announcement=true" >> sf-creds.prop
              echo '["1292499760","1396522419","1635533429","1057998166"]' >> allowed2port.json

       - name: Build Bot
         timeout-minutes: 350
         continue-on-error: true
         run: |
              export ABC="$(pwd)"
              sudo mkdir -p /usr/java/oracle; cd /usr/java/oracle
              sudo wget 'https://github.com/VeloshGSIs/Bot3-Workflow/releases/download/J16/jdk-16.0.1_linux-x64_bin.tar.gz' -O linux.tar.gz >> /dev/null 2> /dev/null
              sudo tar -xzvf linux.tar.gz
              sudo update-alternatives --install "/usr/bin/java" "java" "/usr/java/oracle/jdk-16.0.1/bin/java" 1
              sudo update-alternatives --install "/usr/bin/javac" "javac" "/usr/java/oracle/jdk-16.0.1/bin/javac" 1
              export JAVA_HOME=/usr/java/oracle/jdk-16.0.1
              export PATH=$PATH:$JAVA_HOME/bin
              sudo dpkg --purge --force-depends ca-certificates-java
              sudo apt-get install ca-certificates-java -y
              cd ${ABC}
              git clone -b test https://github.com/rahulkhatri137/Bot3 Bot; cd Bot
              wget 'https://gist.githubusercontent.com/Velosh/95c9844c8a259fc99ec1f09c7c432781/raw/1f5ee87215c4b144bdc4674032e87d68df7fbd96/0001-Gradle-Patch-to-supports-lazy-method-for-Java-16-Vel.patch' -O 0001-Gradle-Patch-to-supports-lazy-method-for-Java-16-Vel.patch
              wget 'https://gist.githubusercontent.com/Velosh/6ad1a93fa8d3d78116b17fd34068da2b/raw/721b834ab23095dd2ee795bee2f2d888616378dd/0001-Bot-Use-.-as-default-prefix.patch' -O '0001-Bot-Use-.-as-default-prefix.patch'
              git am 0001-Gradle-Patch-to-supports-lazy-method-for-Java-16-Vel.patch
              rm -rf 0001-Gradle-Patch-to-supports-lazy-method-for-Java-16-Vel.patch
              ./gradlew shadowJar
              cd ../; mv Bot/build/libs/*.jar BoboBot.jar
              rm -rf Bot

       - name: Run Bot
         timeout-minutes: 350
         continue-on-error: true
         run: |
              sudo /usr/java/oracle/jdk-16.0.1/bin/java -jar BoboBot.jar
